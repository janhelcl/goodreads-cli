# Browser compatibility spike: current evidence

This matrix records observations, not a promise that Goodreads flows work. Never
commit an authenticated page, browser profile, screenshot, cookie, or personal
library/export data. The Goodreads test-account stages in `07-testing-and-delivery.md`
remain gates for login, reads, and mutations.

A public-CLI user-acceptance run on 2026-09-20 is recorded in
[uat-2026-09-20.md](uat-2026-09-20.md). That run used the saved Linux/Chrome
session and did not log out. It confirmed reads, add, start, rate, review,
export, and MCP, and it failed public finish-date editing for a day other than
today. A later liveprobe on the same session set a requested finish date other
than today, freshly verified it, and restored the original unset date.

| Stage | Platform/browser | Evidence | Result |
|---|---|---|---|
| Dedicated profile launch and restart | Linux x86-64, Google Chrome 143.0.7499.40, Rod v0.116.2 | `GOODREADS_BROWSER_TESTS=1 go test ./internal/browser -v` against `httptest.Server`. The 2026-09-20 UAT saw `status --timeout 1ms` return exit 9 because `--version` discovery lost the race and was wrapped as unavailable. After discovery, launch, and lock acquisition keep an expired operation context as timeout/cancellation, a hanging local Chrome fixture with a 40ms deadline returns `timeout` rather than `browser_unavailable`, and three live `status --timeout 1ms --json --debug` retries on this session returned exit 6 with `error_kind=timeout`. | Pass: persistent synthetic cookie survived a clean headless restart. A tiny command timeout during browser discovery is a timeout, not browser unavailable. |
| Local CDP and origin policy | Same | launcher accepts only a loopback CDP URL; synthetic redirect to a different local origin is rejected | Pass |
| Cancellation and shutdown | Same | local Rod component test cancels the operation and waits for Chrome to exit | Pass |
| Browser product pinning | Same | test refuses a different Chromium product for an existing profile and clears the marker with profile deletion | Pass |
| Profile isolation and lock | Linux, `gofrs/flock` v0.13.0 | unit test uses a dedicated temporary profile, verifies permissions, cross-process lock refusal/release, repeat deletion, and symlink refusal | Pass |
| Headed launch, manual sign-in, and saved profile restart | Linux x86-64, Chrome 143.0.7499.40, visible display | User completed Goodreads sign-in in the dedicated profile; a headless restart reached the private library | Pass for this machine/account |
| Authentication detection | Same | Saved profile reached `/review/list/{account}` with exact `My Books` heading, `#books`, `#booksBody`, and sign-out link; a fresh profile was redirected to `/user/sign_in` without these markers; live `gr status --json` and existing-session `gr login --json` succeeded | Pass for saved-session detection; fresh interactive `gr login` remains untested |
| Empty private shelf | Same | Authenticated `#books` and `#booksBody` table had zero rows; live `gr library --json --limit 2` and `gr library --shelf currently-reading --json --limit 2` returned `[]`. The 2026-09-20 UAT also saw the empty `currently-reading` shelf hit the one-minute default timeout twice (CLI exit 6 and MCP `timeout`) before later retries returned `[]` in about four seconds. After `NewPage` stopped waiting for `window.onload` and library reads stopped loading the unfiltered library first, a local Chromium fixture with a never-completing image still returned an empty exclusive shelf, and three live `library --shelf currently-reading --json` retries on this session returned `[]` in about two seconds. | Pass for empty shelves, including empty `currently-reading` under the default timeout |
| Populated shelf structure | Same | Three owner rows established `div.stars[data-rating]`, five semantic star links, review-edit links, editable date wrappers, core/custom shelf links, and filtered headings such as `My Books: Read (2)`; unfiltered, `read`, and empty `currently-reading` reads passed live | Pass for owner rows and filtered/empty shelf reads; populated pagination remains synthetic |
| Exact ISBN lookup | Same plus synthetic fixtures | Checksum normalization, exact ISBN-10/13 match, duplicate rejection, and bounded full scan are tested locally; live rows parse exact ISBNs, but one other edition does not render either ISBN. A visible ISBN search for one absent edition yielded one book route, and expanding that book's visible details exposed the matching ISBN-10 and ISBN-13. Live `get` of a present exact ISBN returned the owner row despite that unidentified sibling; live `get` of a real absent ISBN returned `book_not_found`. The 2026-09-20 UAT reproduced present-ISBN and absent-ISBN `get`, including hyphenated and spaced inputs, but `get` of a plausible public ISBN for the unidentified owner row returned `compatibility` after about 181 seconds. After public identity stopped waiting for Want to Read, stopped remapping lookup timeouts to `library.row`, and matched the already-scanned owner rows, a local owned-book-page fixture without Want to Read resolved the unidentified row by public book ID, and a live retry of that same public ISBN returned `book_not_found` in about 27 seconds because the proved public edition was not the unidentified owner row. A completed public search with no `/book/show/` route now returns `book_not_found` for add of an unrecognized ISBN and for the unidentified-row fallback, instead of waiting until the command deadline. | Pass for unique owner ISBN match, real absence, and clean public-ISBN fallback. A plausible public ISBN that is not the unidentified owner edition is `book_not_found`, not a hung compatibility error. A zero-result public ISBN search is also `book_not_found`. |
| Large-library exact resolution | Linux x86-64, Chrome 143.0.7499.40, Rod v0.116.2; synthetic owner-library pages through the production parser | The owner-library GET search form (`search[query]`, `utf8`) stays on the authenticated shelf but returned zero rows for exact ISBN-10 and ISBN-13, including a present owned edition, so it is not the resolver. Visible public ISBN search proved one stable book ID; owner rows then matched that ID (present: one hit; absent: zero hits) after actual termination. `per_page` options include 10, 20, 30, 40, 50, 75, 100, and `infinite`; 20/50/100/`infinite` applied without leaving the library table. Production requests `per_page=100` and paginates with the 100-page safety budget rather than `infinite`. Synthetic scans still find page-1 and page-11 targets, reject loops/scope changes, and return `scan_incomplete` without mutating. Mutation readback first reloads the resolved owner page and falls back to the terminating exact scan when the row moved. | Pass for the selected visible path: public exact-ISBN book ID, then owner-library ID scan. No live shelf larger than one page has been available; pagination safety remains synthetic. |
| Verified rating mutation | Same | Owner `div.stars[data-rating]` and the full review textarea were observed without a write; live `rate` verified an idempotent 5, changed 5→4, freshly read rating/status/date/custom shelves/full review, restored 4→5, and a separate shelf read confirmed 5. The 2026-09-20 UAT saw the first two public `rate` calls hit the 60-second default (exit 6) without changing the rating; `--timeout 3m` then finished in about 19 seconds. After `rate` stopped loading `Status` before the owner-library scan and bounded the star-control/request waits, a local Chromium fixture with never-completing images still verified a rating change, and live default-timeout `rate` 5→4, `--timeout 1m` restore 4→5, and idempotent `rate` 5 on this session returned `verified:true` in about 18s, 18s, and 4s. | Pass for one exact-ISBN book with an empty review, including under the original one-minute budget; live non-empty-review and post-click ambiguity cases remain pending |
| Status mutation | Same | The owner shelf chooser exposes one floating box with exact `li.visible.exclusive[alt]` controls for all three core statuses and marks the current one with `exclusive_chosen`. Waiting for the browser-initiated Goodreads XHR/fetch to finish before one fresh readback resolved the earlier optimistic-row race. Reversible live canaries verified `read`→`currently-reading`, `read`→`to-read`, `to-read`→`currently-reading`, and restoration; rating, review, finish date, and custom shelves matched on each successful `start` leg. The modern book-page shelf dialog exposes no alternative core-status controls. Returning to `read` creates a finish date and an additional reading session. The 2026-09-20 public `gr add --shelf read` after `start` returned `partial_mutation` (`failed: date`) because that extra session was not removed. A later liveprobe on the same session restored an unset finish date by adding a blank reading-session row when needed, then removing the dated session through its hover-hidden delete control. After status/add stopped loading `Status` before the owner-library scan, retried the chooser open until the exclusive-option contract held, and waited for private-library table markers after `NewPage` saw a document body, public `gr start` then `gr add --shelf read` on an exact edition with an unset finish date returned `verified:true` in about 38s and 71s; a shelf read confirmed `read`, the original rating, and an unset finish date. | Pass for `start` via the legacy owner shelf chooser, and for public `add --shelf read` restoring an unset finish date after `start`. |
| Add mutation | Same | An absent exact ISBN was resolved through visible search and expanded book details. Reversible canaries clicked the scoped `Want to Read` control, waited for its browser request, freshly verified the exact owner row, exercised `to-read`, `currently-reading`, and `read` targets, then used the exact owner-row remove action and freshly verified the original absence by Goodreads book ID. A public `gr add <isbn> --json` smoke test also returned `verified:true` and was restored. The 2026-09-20 UAT added the spec-example ISBN with hyphenated input, verified ISBN-10 `get`, idempotent `add`, `start`, and later liveprobe restoration. | Pass for one previously absent exact edition across all three core statuses. Existing exact owner rows bypass public resolution and use idempotent status semantics; one live idempotent `read` case passed. |
| Finish-date editing | Same | The ordinary review editor exposes per-session year/month/day start and end controls. Goodreads rejects an end date that precedes that session's start date and keeps the previous end date, which is why the 2026-09-20 public CLI could not set a day other than today. A later liveprobe moved the blocking start date first, set a requested finish date other than the machine-local day, and freshly verified it from the exact shelf row. Clearing an unset date by blanking the dropdowns did not persist. Removing the dated session works after ensuring another session row remains: extra sessions are deleted directly; a sole dated session gets a blank companion row first. Hover-hidden delete controls are revealed before click. Two exact editions that still had residual UAT/canary dates were restored to unset through that path. A later public `gr finish --date` on an already-read exact edition for a non-today day returned `verified:true` in about 16 seconds; a shelf read confirmed the requested date, and probe-only clearing restored the unset finish date. | Pass for liveprobe and public CLI set of a non-today finish date, with verified restoration of an unset date. |
| Compound mutation failure reconciliation | Synthetic failure injection plus local and live Linux/Chrome | Failures after new-book add, finish status, and finish date are injected. Verified steps are ordered, exactly one safe final readback produces `partial_mutation`, failed reconciliation produces ambiguity, and no-write/success paths do not reconcile. A local `httptest.Server` flow exercised the real Rod wrapper, production status contract, injected date failure, and safe final readback. A confirmed live canary changed one test edition from `read` to `currently-reading`, injected the later finish-date failure, verified `partial_mutation` with the observed temporary status, and restored the original semantic snapshot. | Pass for fake, local real-browser, and one reversible live partial-failure observation without rollback/replay. |
| Review mutation | Same plus synthetic owner-row and ordinary review-editor fixtures | A reversible live canary replaced one exact edition's review with temporary multiline Unicode text, freshly verified the complete text, restored the original review, and verified edition identity, status, rating, finish date, and custom shelves. The canary and verification errors print no review contents. CLI tests cover `--text`, `--file`, and explicit `--clear`. The 2026-09-20 UAT verified `--text`, multiline/Unicode `--file`, and `--clear` live; a `--file` whose bytes ended in a trailing newline failed readback because that terminator was sent as review text. `--file` now strips one trailing newline (`\n` or `\r\n`) before the review is sent. | Pass for `--text`, `--clear`, and `--file`. Trailing-newline `--file` is covered by CLI tests after the terminator change; public live `gr review --file` with a terminating newline was not re-run. |
| Export | Linux x86-64, Google Chrome 143.0.7499.40, Rod v0.116.2 | The authenticated `/review/import` page exposed one scoped `Export Library` control and separate status/file-list targets. A live canary triggered a new browser request, observed the newly generated `/review_porter/export/{id}/goodreads_export.csv` link, downloaded it through Chromium into a private temporary directory, validated the complete CSV and expected Goodreads headers, atomically installed an 853-byte test-account export, and removed the canary artifact. Local Chromium tests cover browser download events; unit/fixture tests cover freshness proof, unsafe links, malformed CSV, size/type limits, no-overwrite races, and forced replacement. | Pass for one small test-account export on Linux/Chrome |
| Rod-managed Chromium fallback | First-run download and compatibility experiment required | Installed-browser resolution is implemented; automatic download is disabled | Pending |
| macOS and Windows runtime checks | Browser-equipped host or CI runner required | Cross-platform compilation only | Pending |
| Continuous browser integration | GitHub-hosted `ubuntu-24.04`, Google Chrome 152.0.7977.82 | `main` at `1fc167f` passed formatting, vet, tests, race detection, vulnerability scanning, six target builds, and `GOODREADS_BROWSER_TESTS=1` with `GOODREADS_CLI_BROWSER=google-chrome`. The Chrome job ran Rod against local synthetic pages for pagination, exact resolution, rating/readback, ambiguous post-click behavior, and compound partial failure without Goodreads traffic. The equivalent command also passed locally on Linux/Chrome 143.0.7499.40. | Pass for hosted Linux/Chrome component CI |

## Manual account probe

`go run -tags liveprobe ./tools/liveprobe` remains a development-only manual
sign-in and profile-restart probe. It never reads or enters credentials. The
authenticated marker is now part of the adapter and has synthetic tests. The
current private shelf has a small set of test books. `tools/libraryprobe`
records owner-row and filtered-heading structure without printing book data;
`tools/ratingprobe <isbn>` checks the exact target's owner rating control and
review form without printing the book, review, account, or raw HTML;
`tools/resolvepobe <present-isbn> [absent-isbn]` records owner-search, `per_page`,
and public ISBN-to-book-ID observations without printing book data.

Shelf reads return `review` only if a complete review is available; the current
table parser omits it. Missing dates are JSON `null`. Exact-edition `get` scans
the owner library to actual termination or a 100-page safety budget. A unique
ISBN match is enough when no other row shares that book ID. If unidentified
rows remain and no ISBN match exists, `get` uses a visible public ISBN book
page and matches that ID against the owner rows already scanned. Exhausting the budget is
`scan_incomplete`, never not-found.
